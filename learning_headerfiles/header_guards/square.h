#ifndef SQUARE_H //these are so called header guards they make shure, that you don't call the same headerfiles twice
#define SQUARE_H

int getSquareSides();
int getSquarePerimeter(int sideLength);
#endif